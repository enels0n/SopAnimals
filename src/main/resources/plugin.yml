name: ${project.name}
main: net.enelson.sopanimals.SopAnimals
version: ${project.version}
authors: [E_NeLsOn]
depend: [SopLib, PlaceholderAPI]
api-version: 1.20
commands:
  sopanimals:
    aliases: [sanimals, totalanimals]
